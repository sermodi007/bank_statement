<table style="width:100%">
  <thead>
    <tr>
        <%= for key <- @csvkey do  %>
          <th>
            <%= key %> 
          </th>
      <% end %>
      </tr>
  </thead>
  <%= for value <- @decoded_csv do %>
  <tbody>
      <tr> 
          <%= for headkey <- @csvkey do %>
            <th>
            <%= value[headkey]  %>
            <%= %>
            </th>
          <% end %>
      </tr>
  </tbody>
  
  <% end %>

</table>
  <div  class="right">
                <%= Logger.debug "assign Var value: #{inspect(@conn.assigns)}"%>
                <%= link "Preparse", to: page_path(@conn, :preparse)%>
                <%= #Logger.debug "Var value: #{inspect(@conn.assigns.csvkey)}"%>
                <%= #link "Preparse", to: page_path(@conn, :preparse, keys: @csvkey, decodedcsv: @decoded_csv) %>
  </div>
